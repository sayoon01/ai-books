# scripts/ — 그림/표 생성

`results/` + `benchmark/` 집계를 입력으로 논문용 그림과 표를 생성한다.

- `plot_results.py` — `figures/result_bar.pdf` 등 생성 (matplotlib)
- 출력 그림은 `paper/sections/04-results.tex`의 `\framebox` 자리를 대체
